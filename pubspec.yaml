name: realtime_stream_processor
description: Real-time streaming frame processor with camera and ML Kit
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  camera: ^0.10.5+7
  google_mlkit_commons: ^0.6.0
  image: ^4.1.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
